{% extends 'base.html.twig' %}
{% block title %}Modifier {{ programme.nom }}{% endblock %}
{% block body %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1><i class="bi bi-pencil me-2" style="color:#2ecc71;"></i>Modifier le programme</h1>
    <a href="{{ path('app_programme_index') }}" class="btn btn-outline-secondary" style="border-radius:30px;">
        <i class="bi bi-arrow-left"></i> Retour
    </a>
</div>
<div class="row justify-content-center">
    <div class="col-md-7">
        <div class="card card-modern p-4">
            {{ include('programme/_form.html.twig', {'button_label': 'Enregistrer'}) }}
            <div class="mt-3 border-top pt-3">
                {{ include('programme/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
